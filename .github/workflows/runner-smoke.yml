# Temporary diagnostic: checks whether GitHub-hosted runners are available
# to this account. Safe to delete once the build workflow is green.
name: Runner smoke test

on:
  workflow_dispatch:

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ubuntu runner OK"
  windows:
    runs-on: windows-latest
    steps:
      - run: echo "windows runner OK"
